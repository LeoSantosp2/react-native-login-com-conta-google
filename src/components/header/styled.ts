import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  header: {
    width: '100%',
    height: '10%',
    paddingTop: 35,
    paddingLeft: 10,
    flexDirection: 'row',
    alignItems: 'center',
    backgroundColor: '#FFF',
  },

  text: {
    marginLeft: 10,
    fontSize: 18,
  },
});
